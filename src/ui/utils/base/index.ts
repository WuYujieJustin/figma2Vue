import { width } from './width';
import { blendMode } from "./blendMode";
import { bottomLeftRadius } from "./bottomLeftRadius";
import { bottomRightRadius } from "./bottomRightRadius";
import { color } from "./color";
import { fills } from "./fills";
import { height } from "./height";
import { topLeftRadius } from './topLeftRadius';
import { topRightRadius } from './topRightRadius';

export const base = {
    width,
    height,
    fills,
    color,
    topLeftRadius,
    topRightRadius,
    bottomRightRadius,
    bottomLeftRadius,
    blendMode
}